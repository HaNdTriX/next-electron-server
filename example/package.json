{
  "name": "next-electron-server-example",
  "version": "0.0.1",
  "description": "Example using next-electron-server",
  "license": "MIT",
  "main": "main/index.js",
  "scripts": {
    "dev": "concurrently -k \"next dev\" \"electron .\"",
    "build": "next build && next export && electron-builder --dir"
  },
  "build": {
    "asar": true,
    "files": [
      "main",
      "out"
    ]
  },
  "devDependencies": {
    "concurrently": "^5.3.0",
    "electron": "^10.1.3",
    "electron-builder": "^22.8.1",
    "next": "^9.5.3",
    "react": "^16.13.1",
    "react-dom": "^16.13.1"
  },
  "dependencies": {
    "next-electron-server": "file:.."
  }
}
